# Copy to .env and customize for production
MYSQL_ROOT_PASSWORD=root
MYSQL_DATABASE=authentication_system

# Application DB settings (used by PHP via getenv)
DB_HOST=db
DB_NAME=authentication_system
DB_USER=root
DB_PASS=root
DB_PORT=3306
